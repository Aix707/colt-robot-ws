# 待确认问题

以下问题会影响 `colt_trainer` 的数据集、训练强度和发布标准，需要后续确认。

## 训练硬件

已确认：

- 开发机没有 NVIDIA GPU。
- 训练会在有 CUDA 的高性能 Windows 电脑上完成。
- Windows 训练机使用已经调好的默认 Python 环境。
- Windows 训练机性能足够，不需要因训练算力降低模型规模。
- 需要一个独立、方便打包复制到 Windows 的 Python 项目：`/home/xia/桌面/colt_trainer_py`。

非阻塞记录项：

1. Windows 训练机的显卡型号和显存可在第一次训练报告中记录，用于后续复现实验；不阻塞当前开发。

## 椅子与任务场景

已确认：

- 椅子目前 1 种，但会有多把。
- 小车运动时观察角度会有较大变化，干扰多，对模型要求较高。
- 源椅和目标椅之后由 UI 界面人工指定，不由模型自动判断。
- 小车接近椅子的典型距离范围目前不需要考虑，先通过多距离采集覆盖。

非阻塞记录项：

1. 椅面颜色/材质后续在采集 `session.yaml` 中记录，不作为前置约束。

## 小铝块

已确认：

- 小铝块只有一个。
- 小铝块在椅面附近。
- 小铝块大概直径 5 cm、高 5 cm，尺寸只作为弱先验，不要求精确测量。
- 小铝块可以在椅面任意位置。
- 小铝块可能被机械臂、夹爪或椅面边缘遮挡；遮挡时需要云台辅助观察和历史坐标约束。

## 标注方式

已确认：

- 尽量使用强大的模型辅助标注。
- 训练设备性能足够。
- 用户有 LabelMe 的 AI 辅助标注经验。
- 第一轮先采中等数据量，训练能初步识别目标的 v001 模型；后续采集更多数据时用 v001 模型加速半自动标注。

已决策：

- 优先实现 LabelMe JSON、COCO、YOLO segmentation 的导入/导出辅助，方便利用用户已有 LabelMe AI 辅助标注经验。

## 运行时约束

已确认：

- 实测机可以任意安装依赖。
- 实时识别目标频率后续根据实测结果手动调整，不在训练阶段固定。
- 运行环境由开发方决定。
- 默认运行环境：ROS Noetic Python3 + `onnxruntime`。
- TensorRT/OpenVINO 作为后续性能优化，不作为第一版依赖。

## 当前阻塞项

暂无。可以继续推进实测采集、训练数据处理和真实 detector 开发。
